# ZenCore CMS - 系统架构设计

## 1. 系统概述

ZenCore CMS 是一个基于 Nest.js 和 Vue3 的企业级多租户 SaaS 内容管理系统，提供完整的内容管理、模板市场、代理体系和订阅计费功能。

## 2. 技术栈

### 2.1 后端技术栈
- **框架**: Nest.js (Node.js)
- **语言**: TypeScript
- **数据库**: MySQL 8.0+
- **ORM**: TypeORM
- **缓存**: Redis
- **消息队列**: Bull (基于Redis)
- **文件存储**: MinIO / 阿里云OSS
- **搜索引擎**: Elasticsearch (可选)
- **API文档**: Swagger

### 2.2 前端技术栈
- **框架**: Vue 3
- **语言**: TypeScript
- **构建工具**: Vite
- **UI框架**: Element Plus
- **状态管理**: Pinia
- **路由**: Vue Router
- **HTTP客户端**: Axios
- **富文本编辑器**: TinyMCE / Quill
- **Markdown编辑器**: vditor

### 2.3 部署与运维
- **容器化**: Docker & Docker Compose
- **CI/CD**: GitHub Actions
- **监控**: Prometheus + Grafana
- **日志**: ELK Stack (Elasticsearch + Logstash + Kibana)
- **反向代理**: Nginx
- **CDN**: 支持接入第三方CDN

## 3. 系统架构

### 3.1 整体架构图

```
┌─────────────────────────────────────────────────────────────┐
│                          客户端层                            │
├─────────────────┬──────────────┬────────────┬───────────────┤
│   管理后台(PC)   │  移动端Web   │  小程序API  │  第三方客户端  │
└─────────────────┴──────────────┴────────────┴───────────────┘
                              │
                    ┌─────────┴─────────┐
                    │   Nginx / CDN     │
                    └─────────┬─────────┘
                              │
┌─────────────────────────────┴─────────────────────────────┐
│                        应用层 (Nest.js)                     │
├────────────┬─────────────┬─────────────┬──────────────────┤
│ 租户模块    │ 内容模块     │  模板模块    │    代理模块       │
├────────────┼─────────────┼─────────────┼──────────────────┤
│ 用户模块    │ 权限模块     │  订阅模块    │    支付模块       │
└────────────┴─────────────┴─────────────┴──────────────────┘
                              │
┌─────────────────────────────┴─────────────────────────────┐
│                        数据层                               │
├─────────────┬─────────────┬─────────────┬──────────────────┤
│   MySQL     │    Redis    │   MinIO     │  Elasticsearch   │
└─────────────┴─────────────┴─────────────┴──────────────────┘
```

### 3.2 目录结构

```
ZenCore_CMS/
├── backend/                    # 后端项目
│   ├── src/
│   │   ├── modules/           # 业务模块
│   │   │   ├── tenant/        # 租户模块
│   │   │   ├── user/          # 用户模块
│   │   │   ├── auth/          # 认证模块
│   │   │   ├── content/       # 内容模块
│   │   │   ├── template/      # 模板模块
│   │   │   ├── agent/         # 代理模块
│   │   │   ├── subscription/  # 订阅模块
│   │   │   └── payment/       # 支付模块
│   │   ├── common/            # 公共模块
│   │   │   ├── decorators/    # 装饰器
│   │   │   ├── filters/       # 异常过滤器
│   │   │   ├── guards/        # 守卫
│   │   │   ├── interceptors/  # 拦截器
│   │   │   ├── middleware/    # 中间件
│   │   │   └── pipes/         # 管道
│   │   ├── config/            # 配置
│   │   ├── database/          # 数据库
│   │   │   ├── migrations/    # 迁移文件
│   │   │   └── seeds/         # 种子数据
│   │   └── main.ts           # 入口文件
│   ├── test/                  # 测试文件
│   ├── package.json
│   └── tsconfig.json
│
├── frontend/                   # 前端项目
│   ├── admin/                 # 管理后台
│   │   ├── src/
│   │   │   ├── views/         # 页面
│   │   │   │   ├── super-admin/  # 超级管理员
│   │   │   │   ├── agent/        # 代理商
│   │   │   │   └── tenant/       # 租户
│   │   │   ├── components/    # 组件
│   │   │   ├── store/         # 状态管理
│   │   │   ├── router/        # 路由
│   │   │   ├── api/           # API接口
│   │   │   └── utils/         # 工具函数
│   │   └── package.json
│   │
│   └── templates/             # 前台模板
│       ├── default/           # 默认模板
│       └── business/          # 商务模板
│
├── database/                   # 数据库文档
│   ├── schema.sql             # 数据库结构
│   └── init-data.sql          # 初始化数据
│
├── docker/                     # Docker配置
│   ├── docker-compose.yml
│   ├── nginx/
│   └── mysql/
│
├── docs/                       # 文档
│   ├── api/                   # API文档
│   ├── development/           # 开发文档
│   └── deployment/            # 部署文档
│
└── scripts/                    # 脚本
    ├── deploy.sh              # 部署脚本
    └── backup.sh              # 备份脚本
```

## 4. 核心模块设计

### 4.1 多租户模块
- **租户隔离**: 基于租户ID的数据隔离
- **域名绑定**: 支持自定义域名和子域名
- **配额管理**: 存储空间、流量、用户数等配额限制
- **租户配置**: 独立的系统配置和主题设置

### 4.2 用户认证模块
- **JWT认证**: 基于Token的无状态认证
- **角色权限**: RBAC权限控制
- **SSO支持**: 单点登录功能
- **多因素认证**: 支持2FA

### 4.3 内容管理模块
- **可视化编辑**: 富文本和Markdown编辑器
- **版本控制**: 内容变更历史和回滚
- **多语言支持**: i18n国际化
- **审核工作流**: 内容发布审核流程
- **定时发布**: 计划任务发布

### 4.4 模板系统
- **模板引擎**: 支持动态模板渲染
- **模板市场**: 模板上传、审核、购买
- **开发者中心**: 第三方开发者管理
- **版本管理**: 模板版本控制
- **佣金系统**: 自动结算机制

### 4.5 代理体系
- **多级代理**: 支持多层级代理结构
- **区域划分**: 代理区域管理
- **佣金计算**: 灵活的分成规则
- **业绩统计**: 实时数据报表

### 4.6 订阅计费
- **订阅套餐**: 月付/年付套餐
- **计量计费**: 按流量/存储计费
- **增值服务**: 定制服务收费
- **自动续费**: 订阅自动续费
- **发票管理**: 电子发票生成

## 5. 数据库设计原则

### 5.1 多租户数据隔离
- 共享数据库，共享Schema
- 每个表添加 `tenant_id` 字段
- 通过中间件自动注入租户过滤条件

### 5.2 索引优化
- 为常用查询字段添加索引
- 复合索引优化多条件查询
- 定期分析慢查询

### 5.3 数据安全
- 敏感数据加密存储
- 定期自动备份
- 软删除机制

## 6. 安全设计

### 6.1 认证安全
- JWT Token过期机制
- Refresh Token刷新
- 密码加密存储(bcrypt)
- 登录失败限制

### 6.2 接口安全
- 接口限流
- SQL注入防护
- XSS防护
- CSRF防护
- 接口签名验证

### 6.3 数据安全
- 数据加密传输(HTTPS)
- 敏感信息加密存储
- 数据脱敏
- 审计日志

## 7. 性能优化

### 7.1 缓存策略
- Redis缓存热点数据
- HTTP缓存头设置
- CDN加速静态资源

### 7.2 数据库优化
- 读写分离
- 分库分表
- 索引优化
- 查询优化

### 7.3 应用优化
- 异步处理
- 消息队列
- 连接池
- 负载均衡

## 8. 监控与运维

### 8.1 监控指标
- 系统资源监控(CPU、内存、磁盘)
- 应用性能监控(响应时间、吞吐量)
- 业务指标监控(用户量、内容量)
- 错误日志监控

### 8.2 日志管理
- 分级日志记录
- 日志聚合分析
- 日志归档

### 8.3 自动化部署
- Docker容器化
- CI/CD流水线
- 灰度发布
- 自动回滚

## 9. 扩展性设计

### 9.1 水平扩展
- 无状态应用设计
- 支持多实例部署
- 负载均衡

### 9.2 模块化设计
- 微服务架构(可选)
- 插件化系统
- 开放API

### 9.3 数据库扩展
- 主从复制
- 分库分表
- 读写分离

## 10. 测试策略

### 10.1 单元测试
- Jest测试框架
- 覆盖率>80%
- Mock外部依赖

### 10.2 集成测试
- API接口测试
- 数据库集成测试

### 10.3 E2E测试
- Cypress测试
- 关键业务流程测试

### 10.4 性能测试
- 压力测试(支持1000+并发)
- 响应时间<500ms
- 可用性99.9%
